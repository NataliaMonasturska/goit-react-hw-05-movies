import { Link, Outlet, useLocation, useParams } from 'react-router-dom';
export const Cast = () => {
  const params = useParams();
  // console.log(params);
  return (
    <div>
        <p>Actor name</p>
    </div>
  );
};
